# oclw_bot environment (copy to .env and adjust)

# Data path (relative to project root)
DATA_PATH=data/market_cache

# Cache TTL in hours
CACHE_TTL_HOURS=24

# Optional: Yahoo Finance for fetch mode
# YAHOO_RATE_LIMIT_DELAY=0.5

# Optional: broker / execution (future)
# BROKER=mt5
# MT5_PATH=
